apiVersion: v1
kind: Service
metadata:
    name: salvaperismicroservicesnetcoreks
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: salvaperismicroservicesnetcoreks